## Development info

Enter the dev environment with

```sh
nix-shell --pure
```

First time:

```sh
composer install
```

Build the site:

```sh
./vendor/bin/daux generate
```

Watch mode:

```sh
./vendor/bin/daux serve
```
